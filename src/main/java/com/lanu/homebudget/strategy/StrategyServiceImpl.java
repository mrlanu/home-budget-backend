package com.lanu.homebudget.strategy;

import java.math.BigDecimal;
import java.time.LocalDate;

public class StrategyServiceImpl {

        public static void main(String[] args) {
        /*Portfolio portfolio = new Portfolio();
        Market market = new Market();
        market.run(dayQuote -> {
            if (dayQuote.getDate().isAfter(LocalDate.of(2019, 01, 01)) &&
                    dayQuote.getDate().isBefore(LocalDate.of(2020, 01, 01))){
                if (dayQuote.getDate().getDayOfWeek().getValue() == 2){
                    portfolio.buyShares(BigDecimal.valueOf(1000), dayQuote);
                }
                portfolio.countCurrentValue(dayQuote);
            }
        });

        System.out.println(portfolio);
        portfolio.getTrades().forEach(System.out::println);*/
    }

}
